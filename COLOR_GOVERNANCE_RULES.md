# 标准色彩库管理规程

版本：1.0.0  
生效状态：强制受控（只读）  
唯一审批人：系统主管（用户）

---

### 第一章 总则

1. **唯一受控基准**  
   本色彩库为民航统计分析、飞参图表以及技术报告图形呈现的唯一合法色彩数据源。以后所有分析项目、可视化大屏及技术报告必须统一继承此色彩库，严禁在业务脚本中硬编码任意临时十六进制色号。

2. **跨项目继承机制**  
   所有新建或重构的民航数据分析项目，必须通过以下方式之一引入色彩库：
   - Python 项目：通过 `import standard_color_palette as palette` 或统一包路径调用；
   - 前端或报表项目：引入 `color_palette.json` 或 `color_palette.css` 根变量。

---

### 第二章 标准调用准则

1. **点语法强类型调用（推荐基准）**  
   日常单色绘图优先使用点语法属性访问。利用 IDE 自动补全机制选择色标，杜绝拼写错误与色号打错：
   ```python
   import standard_color_palette as palette
   C = palette.C

   # 绘制时直接引用
   ax.plot(x, y, color=C.STEEL_BLUE)
   ```

2. **场景语义角色调用**  
   涉及状态极性、双通道对标或多阶预警时，必须通过语义角色接口取色，实现分析逻辑同具体色相的彻底解耦：
   ```python
   from standard_color_palette import Roles

   # 双通道平行对标
   ax.plot(t, ch1, color=Roles.DualChannel.PRIMARY)
   ax.plot(t, ch2, color=Roles.DualChannel.SECONDARY)
   ```

3. **经典调和搭配矩阵调用**  
   绘制多维柱状图、多阶包线或对比折线时，优先成套调用已经审定的 6 组调和矩阵：
   ```python
   from standard_color_palette import Harmonies

   # 获取五阶包线标准色彩序列
   cols = Harmonies.ENVELOPE_5
   ```

4. **全局工业样式无感注入**  
   图表批量输出前，可通过 `apply_style()` 完成色彩循环与画布基准配置，免去在每个绘图函数中显式传参：
   ```python
   import standard_color_palette as palette

   palette.apply_style()  # 默认应用 S <= 35% 低饱和度基准
   ```

---

### 第三章 色卡更新及授权审批红线

1. **用户唯一审批权准则（刚性红线）**  
   标准色彩库的增、删、改权限严格收口于用户本人。任何智能代理、自动化工具或协作者，**绝对严禁未经用户显式书面确认擅自改动已有色号、重命名色标或新增色卡**。

2. **代码级物理防篡改保护**  
   核心色彩类已被封装为不可变只读容器（Immutable Proxy）。任何在运行时尝试动态修改属性（如 `Colors.STEEL_BLUE = '#123456'`）的操作均会触发 `PermissionError` 物理阻断并强制中断运行。

3. **受控变更四步闭环流程**  
   若业务场景确需引入新色标或调整色相搭配，必须严格执行以下四步审批程序：
   - **第一步：变更提议与冲突论证**  
     提出新增或修改的理由，列明色相、明度及饱和度（必须满足 $S \le 35\%$ 工业克制红线），论证是否与现有 69 种色标发生冲突；
   - **第二步：制作单色局部对比大样**  
     生成包含拟调整色相的高清对照色板，向用户呈报视觉差异；
   - **第三步：用户显式授权确认**  
     在会话中获得用户明确指令授权后，方可开展代码库修订；
   - **第四步：版本升级与基线归档**  
     同步更新 `standard_color_palette.py`、`color_palette.json`、`color_palette.css` 以及《色彩标准大样.png》，递增版本号（如 1.0.0 -> 1.1.0），并在变更日志中留存审计记录。

---

### 第四章 变更审计轨迹

| 版本号 | 生效日期 | 变更简述 | 审批记录 |
| :--- | :--- | :--- | :--- |
| **v1.0.0** | 2026-09-25 | 初始基线确立：完整固化 69 种受控基准色、10 大色彩家族、6 组调和搭配矩阵，启用点语法与运行时只读锁。 | 用户正式审定通过并建立审批红线 |
